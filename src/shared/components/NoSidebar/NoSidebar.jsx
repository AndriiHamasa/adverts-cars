import css from "./NoSidebar.module.css";

const NoSidebar = () => {
  return (
    <div className={css.noSidebarContainer}>
      <h2 className={css.title}>There could be your list</h2>
    </div>
  );
};

export default NoSidebar;
